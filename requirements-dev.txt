# Dependências só de desenvolvimento/testes (não vão para produção).
-r requirements.txt
pytest==9.1.1
flake8==7.3.0
# Acessibilidade (teste tests/test_a11y.py). O teste se auto-pula se estes não
# estiverem instalados, então `pytest` continua verde sem eles.
playwright==1.61.0
axe-core-python==0.1.0
